#!/bin/bash
cd /var/db/pkg
for i in */*/IUSE; do
	if grep lib32 $i>/dev/null; then
		j=$(cat $i)
		echo ${j/ lib32} > $i
	fi
done
for i in */*/IUSE; do
	if grep lib32 $i>/dev/null; then
		j=$(cat $i)
		echo ${j/lib32} > $i
	fi
done
touch */*
