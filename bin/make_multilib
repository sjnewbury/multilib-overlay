#!/bin/bash

make_multilib() {
	sed -i "s/\(src_\(prepare\|compile\|configure\|install\)\)\((\)/multilib-native_\1_internal\3/" $1
	sed -i "/^inherit / { /multilib-native/ b; s/$/ multilib-native/ }" $1
}

make_multilib $1
ebuild $1 manifest
