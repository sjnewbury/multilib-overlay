--- /usr/portage/media-libs/libdc1394/libdc1394-2.0.3.ebuild	2009-01-31 11:54:58.000000000 +0100
+++ ./media-libs/libdc1394/libdc1394-2.0.3.ebuild	2010-05-02 20:26:49.000000000 +0200
@@ -19,7 +19,7 @@
 DEPEND="${RDEPEND}
 	doc? ( app-doc/doxygen )"
 
-src_compile() {
+src_configure() {
 	local myconf=""
 	if use juju; then
 		myconf="--with-juju-dir"
@@ -31,6 +31,9 @@
 		$(use_enable doc doxygen-html) \
 		${myconf} \
 		|| die "econf failed"
+}
+
+src_compile() {
 	emake || die "emake failed"
 	if use doc ; then
 		emake doc || die "emake doc failed"
