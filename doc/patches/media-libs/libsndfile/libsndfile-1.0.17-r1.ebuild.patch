--- /usr/portage/media-libs/libsndfile/libsndfile-1.0.17-r1.ebuild	2010-03-31 04:36:03.000000000 +0200
+++ ./media-libs/libsndfile/libsndfile-1.0.17-r1.ebuild	2010-05-02 17:14:16.000000000 +0200
@@ -22,10 +22,7 @@
 DEPEND="${RDEPEND}
 	dev-util/pkgconfig"
 
-src_unpack() {
-	unpack ${A}
-	cd "${S}"
-
+src_prepare() {
 	epatch "${WORKDIR}/${P}+flac-1.1.3.patch"
 	epatch "${FILESDIR}/${P}-ogg.patch"
 	epatch "${FILESDIR}/${P}-flac-buffer-overflow.patch"
@@ -40,14 +37,13 @@
 	epunt_cxx
 }
 
-src_compile() {
+src_configure() {
 	econf $(use_enable sqlite) \
 		$(use_enable flac) \
 		$(use_enable alsa) \
 		--disable-gcc-werror \
 		--disable-gcc-pipe \
 		--disable-dependency-tracking || die "econf failed."
-	emake || die "emake failed."
 }
 
 src_install() {
