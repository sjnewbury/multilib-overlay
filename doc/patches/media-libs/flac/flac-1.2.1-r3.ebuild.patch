--- /usr/portage/media-libs/flac/flac-1.2.1-r3.ebuild	2010-03-31 04:36:03.000000000 +0200
+++ ./media-libs/flac/flac-1.2.1-r3.ebuild	2010-05-30 22:44:13.000000000 +0200
@@ -29,14 +29,13 @@
 	"${FILESDIR}/${P}-dontbuild-examples.patch"
 	"${FILESDIR}/${P}-gcc-4.3-includes.patch" )
 
-src_unpack() {
-	base_src_unpack
-	cd "${S}"
+src_prepare() {
+	base_src_prepare
 	cp "${WORKDIR}"/*.m4 m4 || die "cp failed"
 	AT_M4DIR="m4" eautoreconf
 }
 
-src_compile() {
+src_configure() {
 	econf $(use_enable ogg) \
 		$(use_enable sse) \
 		$(use_enable 3dnow) \
@@ -47,8 +46,6 @@
 		--disable-doxygen-docs \
 		--disable-dependency-tracking \
 		--disable-xmms-plugin
-
-	emake || die "emake failed."
 }
 
 src_test() {
