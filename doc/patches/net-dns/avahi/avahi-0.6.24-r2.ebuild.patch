--- /usr/portage/net-dns/avahi/avahi-0.6.24-r2.ebuild	2010-03-31 04:36:03.000000000 +0200
+++ ./net-dns/avahi/avahi-0.6.24-r2.ebuild	2010-04-04 03:11:22.000000000 +0200
@@ -98,10 +98,7 @@
 	fi
 }
 
-src_unpack() {
-	unpack ${A}
-	cd "${S}"
-
+src_prepare() {
 	use ipv6 && sed -i -e s/use-ipv6=no/use-ipv6=yes/ avahi-daemon/avahi-daemon.conf
 
 	sed -i -e "s:\\.\\./\\.\\./\\.\\./doc/avahi-docs/html/:../../../doc/${PF}/html/:" doxygen_to_devhelp.xsl
@@ -115,7 +112,7 @@
 	eautoreconf
 }
 
-src_compile() {
+src_configure() {
 	use sh && replace-flags -O? -O0
 
 	local myconf=""
@@ -156,6 +153,9 @@
 		$(use_enable gdbm) \
 		${myconf} \
 		|| die "econf failed"
+}
+
+src_compile() {
 	emake || die "emake failed"
 
 	use doc && emake avahi.devhelp
