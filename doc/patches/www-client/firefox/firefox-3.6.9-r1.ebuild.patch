--- /usr/portage/www-client/firefox/firefox-3.6.9-r1.ebuild	2010-09-08 15:07:16.000000000 +0200
+++ ./www-client/firefox/firefox-3.6.9-r1.ebuild	2010-09-08 22:10:33.000000000 +0200
@@ -121,11 +121,13 @@
 src_unpack() {
 	unpack firefox-${MY_PV}.source.tar.bz2 ${PATCH}.tar.bz2
 
-	linguas
-	for X in ${linguas}; do
-		# FIXME: Add support for unpacking xpis to portage
-		[[ ${X} != "en" ]] && xpi_unpack "${P}-${X}.xpi"
-	done
+	if is_final_abi; then
+		linguas
+		for X in ${linguas}; do
+			# FIXME: Add support for unpacking xpis to portage
+			[[ ${X} != "en" ]] && xpi_unpack "${P}-${X}.xpi"
+		done
+	fi
 }
 
 src_prepare() {
