--- /usr/portage/www-client/firefox/firefox-3.6.8-r1.ebuild	2010-07-25 21:06:36.000000000 +0200
+++ ./www-client/firefox/firefox-3.6.8-r1.ebuild	2010-07-26 14:04:52.000000000 +0200
@@ -119,11 +119,13 @@
 src_unpack() {
 	unpack firefox-${MY_PV}.source.tar.bz2 ${PATCH}.tar.bz2
 
-	linguas
-	for X in ${linguas}; do
-		# FIXME: Add support for unpacking xpis to portage
-		[[ ${X} != "en" ]] && xpi_unpack "mozilla-${P}-${X}.xpi"
-	done
+	if is_final_abi; then
+		linguas
+		for X in ${linguas}; do
+			# FIXME: Add support for unpacking xpis to portage
+			[[ ${X} != "en" ]] && xpi_unpack "mozilla-${P}-${X}.xpi"
+		done
+	fi
 }
 
 src_prepare() {
