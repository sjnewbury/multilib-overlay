--- /usr/portage/app-text/hunspell/hunspell-1.2.8.ebuild	2010-04-06 19:35:32.000000000 +0200
+++ ./app-text/hunspell/hunspell-1.2.8.ebuild	2010-05-30 19:08:29.000000000 +0200
@@ -22,10 +22,7 @@
 
 S=${WORKDIR}/${MY_P}
 
-src_unpack() {
-	unpack ${A}
-	cd "${S}"
-
+src_prepare() {
 	# Upstream package creates some executables which names are too generic
 	# to be placed in /usr/bin - this patch prefixes them with 'hunspell-'.
 	# It modifies a Makefile.am file, hence eautoreconf.
@@ -33,7 +30,7 @@
 	eautoreconf
 }
 
-src_compile() {
+src_configure() {
 	# missing somehow, and I am too lazy to fix it properly
 	[[ ${CHOST} == *-darwin* ]] && append-libs -liconv
 
@@ -43,8 +40,6 @@
 		$(use_enable nls) \
 		$(use_with ncurses ui) \
 		$(use_with readline readline)
-
-	emake || die "emake failed"
 }
 
 src_install() {
