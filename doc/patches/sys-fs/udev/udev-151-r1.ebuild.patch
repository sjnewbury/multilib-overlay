--- /usr/portage/sys-fs/udev/udev-151-r1.ebuild	2010-08-24 20:37:11.000000000 +0200
+++ ./sys-fs/udev/udev-151-r1.ebuild	2010-08-26 01:03:18.000000000 +0200
@@ -24,7 +24,7 @@
 LICENSE="GPL-2"
 SLOT="0"
 KEYWORDS="~alpha ~amd64 ~arm ~hppa ~ia64 ~m68k ~mips ~ppc ~ppc64 ~s390 ~sh ~sparc ~x86"
-IUSE="selinux +devfs-compat +old-hd-rules -extras test"
+IUSE="selinux +devfs-compat +old-hd-rules -extras test introspection"
 
 COMMON_DEPEND="selinux? ( sys-libs/libselinux )
 	extras? (
@@ -35,7 +35,8 @@
 		dev-libs/glib:2
 	)
 	>=sys-apps/util-linux-2.16
-	>=sys-libs/glibc-2.9"
+	>=sys-libs/glibc-2.9
+	introspection? ( >=dev-libs/gobject-introspection-0.6.5 )"
 
 DEPEND="${COMMON_DEPEND}
 	extras? (
@@ -142,9 +143,9 @@
 			mv "${WORKDIR}"/test/sys "${S}"/test/
 		fi
 	fi
+}
 
-	cd "${S}"
-
+src_prepare() {
 	# patches go here...
 
 	# backport some patches
@@ -196,7 +197,7 @@
 	fi
 }
 
-src_compile() {
+src_configure() {
 	filter-flags -fprefetch-loop-arrays
 
 	econf \
@@ -209,10 +210,7 @@
 		--enable-logging \
 		$(use_with selinux) \
 		$(use_enable extras) \
-		--disable-introspection
-	# we don't have gobject-introspection in portage tree
-
-	emake || die "compiling udev failed"
+		$(use_enable introspection)
 }
 
 src_install() {
