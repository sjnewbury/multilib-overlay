--- /usr/portage/net-fs/samba/samba-3.0.37.ebuild	2010-04-25 18:06:42.000000000 +0200
+++ ./net-fs/samba/samba-3.0.37.ebuild	2010-04-26 00:09:50.000000000 +0200
@@ -46,8 +46,7 @@
 	confutils_use_depend_all ads ldap
 }
 
-src_unpack() {
-	unpack ${A}
+src_prepare() {
 	cd "${S}/source"
 
 	# lazyldflags.patch: adds "-Wl,-z,now" to smb{mnt,umount}
@@ -72,7 +71,7 @@
 
 }
 
-src_compile() {
+src_configure() {
 	cd "${S}/source"
 
 	local myconf
@@ -131,13 +130,6 @@
 		$(use_with winbind) \
 		${myconf} ${mylangs} ${mymod_shared}
 
-	emake -j1 proto || die "emake proto failed"
-	emake -j1 everything || die "emake everything failed"
-
-	if use python ; then
-		emake -j1 python_ext || die "emake python_ext failed"
-	fi
-
 	if use oav ; then
 		# maintainer-info:
 		# - there are no known releases of mks or kavdc,
@@ -152,6 +144,21 @@
 			--with-filetype \
 			--with-fileregexp \
 			$(use_enable debug)
+	fi
+}
+
+src_compile() {
+	cd "${S}/source"
+
+	emake -j1 proto || die "emake proto failed"
+	emake -j1 everything || die "emake everything failed"
+
+	if use python ; then
+		emake -j1 python_ext || die "emake python_ext failed"
+	fi
+
+	if use oav ; then
+		cd "${WORKDIR}/${VSCAN_P}"
 		emake -j1 || die "emake oav plugins failed"
 	fi
 }
